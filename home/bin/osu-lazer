#!/usr/bin/env zsh

sudo -A raw-evdev-abs-input /dev/input/$(
  egrep Stylus /sys/class/input/event*/device/name | cut -d : -f 1 | egrep -o "event[0-9]+"
) inverted &

/usr/bin/osu-lazer

sudo -A pkill -f raw-evdev-abs-input
